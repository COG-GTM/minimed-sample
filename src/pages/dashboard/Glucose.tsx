import { Activity } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';

const Glucose = () => (
  <PlaceholderPage
    icon={Activity}
    title="Glucose Monitoring"
    description="Track your continuous glucose readings, trends, and time in range."
  />
);

export default Glucose;
