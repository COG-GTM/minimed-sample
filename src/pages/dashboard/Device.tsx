import { Wifi } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';

const Device = () => (
  <PlaceholderPage
    icon={Wifi}
    title="Device Status"
    description="Monitor your pump connectivity, battery level, and reservoir status."
  />
);

export default Device;
