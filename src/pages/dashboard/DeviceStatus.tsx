import { Wifi } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';

const DeviceStatus = () => (
  <PlaceholderPage
    icon={Wifi}
    title="Device Status"
    description="Monitor pump connectivity, battery, and reservoir levels"
  />
);

export default DeviceStatus;
