import { Settings as SettingsIcon } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';

const Settings = () => (
  <PlaceholderPage
    icon={SettingsIcon}
    title="Settings"
    description="Manage your account preferences, notifications, and device configuration."
  />
);

export default Settings;
