import { Settings } from 'lucide-react';
import PlaceholderSection from './PlaceholderSection';

const SettingsSection = () => (
  <PlaceholderSection
    icon={Settings}
    title="Settings"
    description="Configure your account preferences and app settings"
  />
);

export default SettingsSection;
