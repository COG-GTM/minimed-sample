import PlaceholderSection from './PlaceholderSection';

const DeviceStatusSection = () => (
  <PlaceholderSection
    title="Device Status"
    description="Check pump connectivity, battery, reservoir, and sensor status."
  />
);

export default DeviceStatusSection;
