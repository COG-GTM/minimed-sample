import { FileText } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';

const Reports = () => (
  <PlaceholderPage
    icon={FileText}
    title="Reports"
    description="Generate and export diabetes management reports"
  />
);

export default Reports;
