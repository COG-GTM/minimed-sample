import { FileText } from 'lucide-react';
import PlaceholderPage from './PlaceholderPage';

const Reports = () => (
  <PlaceholderPage
    icon={FileText}
    title="Reports"
    description="Generate and download summary reports to share with your care team."
  />
);

export default Reports;
