import { Wifi } from 'lucide-react';
import PlaceholderPage from '@/pages/PlaceholderPage';

const Devices = () => (
  <PlaceholderPage
    title="Device Status"
    description="Pump, sensor, and transmitter status and pairing"
    icon={Wifi}
  />
);

export default Devices;
