import { Settings } from 'lucide-react';
import PlaceholderPage from '@/pages/PlaceholderPage';

const SettingsPage = () => (
  <PlaceholderPage
    title="Settings"
    description="Application preferences and device configuration"
    icon={Settings}
  />
);

export default SettingsPage;
