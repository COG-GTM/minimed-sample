import { FileText } from 'lucide-react';
import PlaceholderPage from '@/pages/PlaceholderPage';

const Reports = () => (
  <PlaceholderPage
    title="Reports"
    description="Downloadable summaries and clinical reports"
    icon={FileText}
  />
);

export default Reports;
